<%= form_with(model: used_disk) do |form| %>
  <% if used_disk.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(used_disk.errors.count, "error") %> prohibited this used_disk from being saved:</h2>

      <ul>
        <% used_disk.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
